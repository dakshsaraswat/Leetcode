class Solution:
    def rotateString(self, s: str, goal: str) -> bool:
        # Check if lengths match and if goal is a substring of s duplicated
        return len(s) == len(goal) and goal in (s + s)
