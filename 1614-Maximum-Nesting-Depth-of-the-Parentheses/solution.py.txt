class Solution:
    def maxDepth(self, s: str) -> int:
        ans = 0
        curr_depth = 0
        for char in s:
            if char == '(':
                curr_depth += 1
                ans = max(ans, curr_depth)
            elif char == ')':
                curr_depth -= 1
        return ans